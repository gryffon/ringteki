const DrawCard = require('../../drawcard.js');

class AppealingToTheFortunes extends DrawCard {
    setupCardAbilities(ability) { // eslint-disable-line no-unused-vars
    }
}

AppealingToTheFortunes.id = 'appealing-to-the-fortunes'; // This is a guess at what the id might be - please check it!!!

module.exports = AppealingToTheFortunes;
